[Unit]
Description=Inkstand Browser

[Service]
Type=simple
ExecStart=/usr/bin/chromium-browser --no-sandbox --headless --disable-gpu --hide-scrollbars --remote-debugging-port=9222

[Install]
WantedBy=multi-user.target